= simple_form_for(@workout, html: { class: 'form-horizontal' }) do |f|
	= f.input :date, label: "Date"
	= f.input :workout, label: "Muscle Group", input_html: { class: "form-control" }
	= f.input :mood, label: "Mood During Workout", input_html: { class: "form-control" }
	= f.input :length, label: "Length of Workout", input_html: { class: "form-control" }
	%br/
	= f.button :submit